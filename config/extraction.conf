{
    # Filter the rows to generate report
    "filters" : [
        {
            #Filters can be aggregated
            "and|or": {
                "column_name": "",
                "column_data_type": "date|number|string",
                "condition": [
                    {
                        "and|or": {
                            "contains|prefix|suffix|match|greater|lesser|not": ""
                        }
                    }
                ]   
            }
        }
    ]
}